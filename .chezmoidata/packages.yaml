packages:
  linux:
    dnf:
      - gh
    custom:
      - name: zed
        check: "which zed"
        install: "curl -f https://zed.rs/install.sh | sh"

  darwin:
    brew:
      - zed
      - gh

  windows:
    winget:
      - ZedIndustries.Zed
      - GitHub.cli
